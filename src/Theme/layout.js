import { StyleSheet } from 'react-native'

const layout = {
    container: {
        flex: 1,
    },
    row: {
        flexDirection: 'row',
    },
    padding_thin: {
        padding: 5
    },
    padding_thick: {
        padding: 10
    },
    centered: {
        justifyContent: 'center',
        alignItems: 'center'
    }
}

export default layout